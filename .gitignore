# Python bytecode (audio pipeline tooling in scripts/)
__pycache__/
*.py[cod]

# Local-only issue-tracking state (regenerated by scripts/create_issues.py)
scripts/.issue_map.json

# Agent-isolated worktrees (transient; created by /review-lesson and friends)
.claude/worktrees/

# Per-session scratchpads (review-run todos, handoffs, ad-hoc TODOs)
tasks/
